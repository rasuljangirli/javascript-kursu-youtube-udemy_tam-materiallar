
//! ClipBoard events

const input = document.querySelector("#txtTaskName");

//? copy
// document.addEventListener("copy", logEvent);

// input.addEventListener("copy", logEvent);

// function logEvent(e) {
// e.preventDefault();
// console.log(e.target);
// console.log(window.getSelection().toString());
// console.log(e.target.value.slice(e.target.selectionStart, e.target.selectionEnd));
// e.clipboardData.setData("text", "No Hello");
// }

//? paste

// input.addEventListener("paste", logEvent);

// function logEvent(e) {
// e.preventDefault();
// console.log(e.target)
// console.log(e.clipboardData.getData("text"));
// }

//? cut

// document.addEventListener("cut", logEvent);

// function logEvent(e) {
// console.log(e.target)

// console.log(e.target.value.slice(e.target.selectionStart, e.target.selectionEnd));

// console.log(window.getSelection().toString());

// }




























